# Dependências
node_modules
scripts/whatsapp-catalog/node_modules/
/.pnp
.pnp.js

# Next.js build
/.next/
/out/
/build

# Variáveis de ambiente (NUNCA commitar)
.env
.env.local
.env*.local

# Misc
.DS_Store
*.pem
Thumbs.db

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Firebase service account (se baixar localmente)
*-firebase-adminsdk-*.json

# Firebase deploy cache
.firebase/

# Logs de debug do emulador (gerados por `npm run test:rules`)
firebase-debug.log
firestore-debug.log
ui-debug.log

# Backup local de secrets/config antes de apagar recursos do GCP — nunca comitar
backup-apphosting-*/
